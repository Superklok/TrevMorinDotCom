version: "3.8"

services:
  app:
    image: superklok/trevmorindotcom:1.8.8
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production