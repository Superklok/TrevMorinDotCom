version: "3.8"

services:
  app:
    image: trevmorindotcom:1.8.7
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production